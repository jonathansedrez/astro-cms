# Astro cms

## Configure localstack

`docker run -d -p 4566:4566 --name localstack localstack/localstack:3.8.0` -> Setting a version to garantee it is a free tier.

## Terraform install

```
brew tap hashicorp/tap
brew install hashicorp/tap/terraform
```

## Terraform init
```
terraform init
```